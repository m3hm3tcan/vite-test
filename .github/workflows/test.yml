on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'
        required: true
        default: 'info'
        type: choice
        options:
        - info
        - warning
        - debug
      projectName:
        description: 'Project Name'
        required: true
env:
  LEVEL: ${{ inputs.logLevel }}
  PROJECTNAME: ${{ inputs.projectName }}
jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/testing'
    steps:
      - run: |
          echo "Log level: $PROJECTNAME"
          echo "Log level: $LEVEL"
